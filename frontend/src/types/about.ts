export interface FounderDetails {
  portraitUrl: string;
  quote: string;
  quoteAuthor: string;
  quoteRole: string;
  eyebrow: string;
  title: string;
  bio: string;
  caption: string;
  mission: string;
  vision: string;
}

export interface StatItem {
  value: string;
  label: string;
}

export interface MilestoneStat {
  number: string;
  label: string;
}
